title: Display template
directive: display-template
imports:
  DisplayTemplate:
    fidelity:
      withDefinition: true
  DisplayLine:
    fidelity:
      withDefinition: true
  DisplayLineElement:
    fidelity:
      withDefinition: true
  DataElement:
    fidelity:
      withDefinition: true
  TextElement:
    fidelity:
      withDefinition: true