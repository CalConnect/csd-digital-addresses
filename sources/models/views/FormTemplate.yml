title: Form template
directive: ix-form-template
imports:
  FormTemplate:
    fidelity:
      withDefinition: true
  FormLine:
    fidelity:
      withDefinition: true
  FormLineElement:
    fidelity:
      withDefinition: true
  SelectionPair:
    fidelity:
      withDefinition: true
  SelectionElement:
    fidelity:
      withDefinition: true
  DataElement:
    fidelity:
      withDefinition: true
  StaticTextElement:
    fidelity:
      withDefinition: true
  DataElementWithConditions:
    fidelity:
      withDefinition: true